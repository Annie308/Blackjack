#include "global.h"
#include "training.h"

/* Plays the actual game of blackjack based on q_table. Does not update the table or explore. */


/*
    open cv???
        interpret the image from the camera
        return the state 

    game()
        get the state from open cv
        get the action from get_action()

        tell the arduino to perform the action
*/